
<br/>
<div class="wrapper mt-5 col-md-9 offset-md-2">
  <h2 class="mt-3">Your Tours</h2>

<% if current_user.bookings.empty? %>
  <p>You don't have any upcoming tours. <%= link_to "Start exploring!", tours_path, class: 'btn btn-info' %> </p>
<% else %>
  <ul class='list-group booking_list'>
    <% current_user.bookings.each_with_index do |booking, index| %>
    <li class="my-4">
      <h3 class="border-bottom pb-3"><%= index + 1 %> <span style="font-size: 18px;"><i class="fas fa-mountain"></i></span> <%= booking.tour.name%></h3>
      <div>
         <strong>Date: </strong><%= booking.nice_date %> - <strong>People:</strong> <%= booking.headcount %> - <strong>Price: ￥</strong> <%= booking.tour.cost * booking.headcount %> - <strong>Total:</strong>
         <%= link_to raw('<i class="fas fa-trash-alt"></i>'), booking_path(booking), method: "delete", data: { confirm: "Are you sure you want to cancel this tour?" }%>
      </div>
      <div class="content_detail d-flex justify-content-between mt-3">
        <%= cl_image_tag booking.tour.photo%>
        <p class='ml-3'><%= booking.tour.details%></p>
      </div>


    </li>
  <% end %>
  </ul>
<% end %>


</div>

